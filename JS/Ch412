//let n = 0; while(n < 91) {console.log(n); n+=10;}

//while(condition) {block of code}

let isOver = false;
let counter = 1;
 
while (!isOver) {
    isOver = !confirm(`[${counter++}] Continue the loop?`);
}
